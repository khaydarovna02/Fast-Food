import React from "react";
import { Container, DeleteIcon, DeleteWrapper } from "./style";

export const Delete = () => {
  return (
    <Container>
      <DeleteWrapper>
        <DeleteIcon />
      </DeleteWrapper>
    </Container>
  );
};

export default Delete;
