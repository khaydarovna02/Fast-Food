import React from "react";
import { Container, CancelIcon } from "./style";

export const Cancel = () => {
  return (
    <Container>
      <CancelIcon />
    </Container>
  );
};

export default Cancel;
